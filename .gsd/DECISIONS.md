# Decisions Register

<!-- Append-only. Never edit or remove existing rows.
     To reverse a decision, add a new row that supersedes it.
     Read this file at the start of any planning or research phase. -->

| # | When | Scope | Decision | Choice | Rationale | Revisable? | Made By |
|---|------|-------|----------|--------|-----------|------------|---------|
| D001 | M003/S01 | arch | Fix hair SVG paths that extend far beyond the visible hair region | Clip or redraw hair paths so they are bounded to the hair zone (~y 0-290) instead of spanning the full character (y 59-970). This prevents hue-rotate from visually recoloring body/skin/tail areas through the underlying hair path. | User override: the current vtracer-traced hair paths in mermaid-{1..9}.svg extend behind the body and tail, so CSS hue-rotate (applied to the whole SVG) shifts hair color in regions the user perceives as skin or tail. The fix is to ensure hair paths are geometrically bounded to only the hair-visible region. | No | human |
| D002 | M004 | arch | Dress-up to coloring conversion method | Pre-generate 9 static B&W outline SVGs matching dress-up characters, bundle as assets. No runtime AI generation. | There are exactly 9 fixed characters (no part-mixing yet). Pre-generating makes "Color This!" instant with zero API cost. Live generation reserved for future part-mixing feature. | Yes | collaborative |
| D003 | M004 | arch | Dress-up to coloring navigation flow | "Color This!" button in dress-up navigates directly to coloring canvas with matching outline — bypasses gallery. | Direct button is the most intuitive for a 6-year-old. No gallery detour — tap and color. The character identity (which of 9 mermaids) carries over; hue-rotate color is irrelevant since coloring pages are B&W. | Yes | collaborative |
| D004 | M004 | scope | Icon refresh scope | Redo all icons — nav bar (3) and home screen activity buttons (2). Semantically meaningful: mermaid tail for dress-up, palette for coloring, house for home. | The dress-up star icon makes no sense for "dress up your mermaid." A 6-year-old navigates by icon recognition. Refreshing all icons together ensures visual consistency. | Yes | collaborative |
| D005 | M004/S01 | arch | Fix open hair SVG paths in dress-up characters during M004 | Clip or redraw hair paths in all 9 mermaid-{1-9}.svg dress-up character SVGs so hair geometry is bounded to the visible hair region (~y 0-290). This fix is bundled into M004 alongside the dress-up → coloring pipeline work. Supersedes D001's framing as a standalone fix — now explicitly scoped into M004/S01. | User override: the vtracer-traced hair paths extend far behind the body and tail, so CSS hue-rotate (applied per-region) visually shifts color in areas the user perceives as skin or tail. D001 identified the problem but left it unscheduled. The user directed that M004 is the right time to fix this since we're already working with dress-up SVG assets for the coloring outline pipeline. Fixing hair paths ensures both hue-rotate recoloring AND the new coloring outlines work correctly. | No | human |
| D006 | M004/S03 | art-pipeline | How to provide coloring page art for pages 5-9 without an OpenAI API key | Generate geometric placeholder SVGs using scripts/generate_coloring_placeholders.py — shared mermaid body template + per-scene elements (forest, treasure, jellyfish, whirlpool, starfish). Each SVG has 18-28 paths, well above the ≥5 threshold. Script auto-detects stubs (<500 bytes) and is idempotent for real art. | No OpenAI API key available for the AI pipeline. Geometric placeholders follow the same pattern as generate_dressup_outlines.py --placeholder and are functionally adequate for gallery display (non-blank thumbnails, colorable line art). The content-quality test guards against regression. Real AI art can be generated later by running the pipeline with an API key — the script won't overwrite non-stub files. | Yes — regenerate with AI pipeline when OPENAI_API_KEY is available | agent |
