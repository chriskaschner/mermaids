---
phase: 07-github-pages-deployment
task: gap-closure
total_tasks: 3
status: blocked
last_updated: 2026-03-12T01:27:26.722Z
---

<current_state>
Phase 7 execution is complete but verification found 2 gaps. The site is live at https://mermaids.chriskaschner.com with DNS and CI test gate working, but DPLY-03 (iPad Safari UAT) failed. Dress-up feature is broken on the live static site. Next step is gap closure planning.
</current_state>

<completed_work>

- Plan 07-01: CI test gate — DONE (commits a234d2e, ce2fbb3, 04fa7a4)
  - BASE_URL env var guard added to tests/conftest.py live_server fixture
  - CI test job added to deploy.yml running E2E tests against python -m http.server
  - deploy job gated on test job via needs: [test]
  - Note: 2 pre-existing webkit failures deferred (test_tap_region_triggers_sparkle, test_sparkle_cleanup)

- Plan 07-02: Custom domain + iPad Safari verification — DONE with gaps (commits a01682e, d220043, 28edf68, d5de22a)
  - frontend/CNAME created with mermaids.chriskaschner.com
  - DNS CNAME record live (mermaids.chriskaschner.com -> chriskaschner.github.io)
  - GitHub Pages custom domain configured, DNS check green
  - iPad Safari UAT FAILED — dress-up broken (see gaps below)

- VERIFICATION.md created at .planning/phases/07-github-pages-deployment/07-VERIFICATION.md
  - Status: gaps_found
  - Score: 2/3 must-haves

</completed_work>

<remaining_work>

Gap 1 (DPLY-03 — blocking): Dress-up broken on live GitHub Pages site
  - Tapping tail/hair/accessory shows full mermaid list instead of swapping part
  - Color swatch recolors broad area instead of selected part only
  - Root cause undiagnosed — likely JS error or path issue on static hosting vs uvicorn
  - First diagnostic: open DevTools on iPad Safari at https://mermaids.chriskaschner.com

Gap 2 (secondary): HTTPS not enforced
  - http://mermaids.chriskaschner.com returns HTTP 200 with no redirect to HTTPS
  - "Enforce HTTPS" checkbox in GitHub Pages Settings may not be active yet

</remaining_work>

<decisions_made>

- BASE_URL env var approach for conftest.py: minimal change, works with any static server, zero overhead when not set
- CI uses python -m http.server 8080 with readiness poll (30 attempts, 1s sleep) before running Playwright
- 2 pre-existing webkit sparkle test failures deferred (not caused by phase 7 changes, fail identically vs uvicorn)
- Task 3 (iPad Safari) marked FAILED with issues recorded rather than auto-passing

</decisions_made>

<blockers>

- DPLY-03 gap: Dress-up feature broken on live static site — root cause unknown
  - Hypothesis: JS error related to static asset paths, missing API endpoint (app may call backend that doesn't exist on static hosting), or event handler differences in Safari
  - First step: check browser console on iPad Safari DevTools

</blockers>

<context>
Phase 7 is essentially a deployment + verification phase. The infrastructure work (CI gate, DNS, custom domain) all landed cleanly. The remaining problem is that the dress-up UI feature that works locally/in E2E tests against uvicorn is broken on the live GitHub Pages static site. This is likely because the app has some backend dependency (FastAPI endpoint) that works locally but isn't available in the static GitHub Pages deployment — or a Safari-specific JS issue. The gap closure plan should focus on diagnosing and fixing the static hosting compatibility.
</context>

<next_action>
Run: /gsd:plan-phase 7 --gaps
This reads VERIFICATION.md, creates gap closure plans targeting DPLY-03 and HTTPS enforcement.
After /clear to get fresh context window.
</next_action>
