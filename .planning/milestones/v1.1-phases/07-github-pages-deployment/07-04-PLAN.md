---
phase: 07-github-pages-deployment
plan: 04
type: execute
wave: 2
depends_on:
  - 07-03
files_modified: []
autonomous: false
requirements:
  - DPLY-01
  - DPLY-02
  - DPLY-03
gap_closure: true

must_haves:
  truths:
    - "Dress-up part swap works on real iPad Safari: tapping a variant option replaces the displayed character"
    - "Dress-up color recolor works on real iPad Safari: tapping a swatch recolors only the active character"
    - "Coloring activity works on real iPad Safari: tap-to-fill and undo function correctly"
    - "HTTP redirects to HTTPS: curl -sI http://mermaids.chriskaschner.com shows 301 redirect"
  artifacts: []
  key_links:
    - from: "GitHub Pages Settings"
      to: "HTTPS enforcement"
      via: "Enforce HTTPS checkbox in Settings > Pages"
      pattern: "301.*https"
---

<objective>
Verify dress-up fix on real iPad Safari and enable HTTPS enforcement (closing both VERIFICATION.md gaps).

Purpose: Plan 07-03 applied z-index and event propagation fixes to address the iPad Safari dress-up interaction bug. This plan verifies those fixes work on a real device (Gap 1) and enables HTTPS enforcement (Gap 2). Both gaps from VERIFICATION.md are closed by this plan.

Output: DPLY-03 satisfied (iPad Safari acceptance), HTTPS enforced.
</objective>

<execution_context>
@/Users/chriskaschner/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chriskaschner/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/07-github-pages-deployment/07-VERIFICATION.md
@.planning/phases/07-github-pages-deployment/07-03-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Verify dress-up and coloring on real iPad Safari</name>
  <files>n/a</files>
  <action>
Human verifies the live site on a real iPad Safari device after Plan 07-03 deployed z-index and stopPropagation fixes. This is the final DPLY-03 acceptance gate.

Plan 07-03 applied these fixes to the live site (deployed via CI):
- CSS z-index stacking: selection panel (z-index: 20) now sits above nav bar (z-index: 5), preventing taps on option buttons from hitting nav links underneath
- JS stopPropagation guards on all selection panel click handlers (option buttons, color swatches, category tabs, undo)
- Debug overlay available via ?debug=1 query param for diagnostics
  </action>
  <verify>
    <automated>curl -sf https://mermaids.chriskaschner.com/ | head -5</automated>
  </verify>
  <done>Both dress-up (part swap + color recolor) and coloring (tap-to-fill + undo) work correctly on real iPad Safari.</done>
  <what-built>
Plan 07-03 applied CSS z-index stacking and JS stopPropagation guards to fix touch interaction on the selection panel.
  </what-built>
  <how-to-verify>
**Step 1: Verify dress-up (DPLY-03 primary test)**
1. On a real iPad running Safari, open: https://mermaids.chriskaschner.com#/dressup
2. Wait for the mermaid to load
3. Tap the Tails tab (fish tail icon) -- confirm 3 option buttons appear
4. Tap a different tail option -- confirm the displayed mermaid changes (not navigates away)
5. Tap the Hair tab -- confirm 3 hair options appear
6. Tap a different hair option -- confirm the displayed mermaid changes
7. Tap the Accessories tab -- confirm 3 accessory options appear
8. Tap the Color tab (rainbow icon) -- confirm color swatches appear
9. Tap a color swatch -- confirm the mermaid's colors change (the active character gets recolored)

**Step 2: Verify coloring (DPLY-03 secondary test)**
1. Navigate to Coloring (tap coloring nav icon or go to https://mermaids.chriskaschner.com#/coloring)
2. Tap a coloring page thumbnail
3. Select a color swatch, then tap a white region -- confirm it fills with the selected color
4. Tap Undo -- confirm the fill reverts

**Step 3: If anything fails, use debug overlay**
1. Open https://mermaids.chriskaschner.com?debug=1#/dressup
2. The debug overlay at the top shows what element receives each tap
3. Try the failing action and report what the overlay shows (e.g., "target=A.nav-icon" would confirm the nav bar is intercepting taps)

**Expected behavior:**
- Dress-up: tapping option buttons swaps the character, NOT navigates away
- Color: tapping swatches recolors the active character
- Coloring: tap-to-fill works, undo works
- No blank screens, no JS errors visible in debug overlay
  </how-to-verify>
  <resume-signal>Type "verified" if dress-up and coloring both work. If issues remain, describe what happens and paste any debug overlay output.</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Enable HTTPS enforcement in GitHub Pages Settings</name>
  <files>n/a</files>
  <action>
Human enables HTTPS enforcement in GitHub Pages repo settings. This closes Gap 2 from VERIFICATION.md (HTTP does not redirect to HTTPS).

Steps:
1. Go to https://github.com/chriskaschner/mermaids/settings/pages
2. Find the "Enforce HTTPS" checkbox
3. If not already checked, check it and save
4. If the checkbox is grayed out, note this and check back later
  </action>
  <verify>
    <automated>curl -sI http://mermaids.chriskaschner.com/ | head -5</automated>
  </verify>
  <done>Enforce HTTPS checkbox is checked in GitHub Pages Settings. HTTP requests redirect to HTTPS (may take up to 24h to activate).</done>
  <what-to-do>
**Enable HTTPS redirect (Gap 2 from VERIFICATION.md):**

1. Go to https://github.com/chriskaschner/mermaids/settings/pages
2. Find the "Enforce HTTPS" checkbox
3. If not already checked, check it and save
4. If the checkbox is grayed out with a message about waiting for certificate provisioning, note this and check back in a few hours

**Verify:**
After enabling, run this in a terminal:
```
curl -sI http://mermaids.chriskaschner.com/ | head -5
```

Expected output should show a 301 redirect:
```
HTTP/1.1 301 Moved Permanently
Location: https://mermaids.chriskaschner.com/
```

If it still shows `HTTP/1.1 200 OK` with no redirect, GitHub may need up to 24 hours to provision the HTTPS certificate and enable enforcement.
  </what-to-do>
  <resume-signal>Type "https-enabled" once the Enforce HTTPS checkbox is checked (even if the redirect is not yet active -- it can take up to 24h).</resume-signal>
</task>

</tasks>

<verification>
Phase 7 gap closure complete when:
- Dress-up works on real iPad Safari (part swap + color recolor)
- Coloring works on real iPad Safari (tap-to-fill + undo)
- HTTPS enforcement is enabled (checkbox checked; redirect may take up to 24h to activate)
- All automated CI tests pass on main branch
</verification>

<success_criteria>
- DPLY-03 satisfied: both activities work on real iPad Safari
- HTTPS enforcement checkbox enabled in GitHub Pages Settings
- curl http://mermaids.chriskaschner.com shows 301 redirect (or will within 24h)
- All VERIFICATION.md gaps are closed
</success_criteria>

<output>
After completion, create `.planning/phases/07-github-pages-deployment/07-04-SUMMARY.md`
</output>
