---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - src/mermaids/__init__.py
  - src/mermaids/app.py
  - frontend/index.html
  - frontend/css/style.css
  - tests/conftest.py
  - tests/test_app.py
autonomous: true
requirements:
  - FOUN-02
  - FOUN-01

must_haves:
  truths:
    - "FastAPI serves the frontend directory as a static SPA at http://localhost:8000"
    - "HTML shell includes iPad-optimized viewport meta, web-app-capable tags, and full-screen CSS"
    - "Test infrastructure runs and a basic test passes"
  artifacts:
    - path: "pyproject.toml"
      provides: "Project config with all dependencies"
      contains: "fastapi"
    - path: "src/mermaids/app.py"
      provides: "FastAPI application with static file mount"
      exports: ["app"]
    - path: "frontend/index.html"
      provides: "SPA shell with iPad viewport, nav placeholder, app container"
      contains: "apple-mobile-web-app-capable"
    - path: "frontend/css/style.css"
      provides: "iPad full-screen CSS foundation with 60pt tap target base styles"
      contains: "touch-action"
    - path: "tests/conftest.py"
      provides: "Shared test fixtures for FastAPI and Playwright"
    - path: "tests/test_app.py"
      provides: "FastAPI static serving tests"
  key_links:
    - from: "src/mermaids/app.py"
      to: "frontend/"
      via: "StaticFiles mount with html=True"
      pattern: 'StaticFiles.*html=True'
---

<objective>
Scaffold the project: Python package with FastAPI serving a static frontend, iPad-optimized HTML/CSS shell, and test infrastructure with pytest + Playwright.

Purpose: Everything in Phase 1 depends on having a running server, a valid HTML shell, and working tests. This is the foundation under the foundation.
Output: A running FastAPI app at localhost:8000 serving an iPad-ready HTML page, with passing tests.
</objective>

<execution_context>
@/Users/chriskaschner/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chriskaschner/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Project scaffolding and FastAPI static server</name>
  <files>pyproject.toml, src/mermaids/__init__.py, src/mermaids/app.py, tests/test_app.py</files>
  <behavior>
    - Test: GET / returns 200 with HTML content-type
    - Test: GET /css/style.css returns 200 with CSS content-type
    - Test: GET /nonexistent returns 404
    - Test: App has CORS headers or no CORS needed (static only)
  </behavior>
  <action>
    1. Create pyproject.toml with uv-compatible project config:
       - name: mermaids
       - python: ">=3.11"
       - dependencies: fastapi>=0.115, uvicorn>=0.34, vtracer>=0.6.12, pillow>=11
       - dev dependencies: pytest>=8, playwright>=1.49, pytest-playwright>=0.6, httpx>=0.28
    2. Run `uv sync` to install all dependencies
    3. Run `uv run playwright install webkit` to install the WebKit browser for iPad testing
    4. Create src/mermaids/__init__.py (empty)
    5. Create src/mermaids/app.py:
       - FastAPI() instance
       - Mount "/" with StaticFiles(directory=Path(__file__).parent.parent.parent / "frontend", html=True)
       - The mount must be last so future API routes take precedence
    6. Create a minimal frontend/index.html placeholder (just enough for tests -- full content in Task 2)
    7. Create a minimal frontend/css/style.css placeholder
    8. Create tests/conftest.py with:
       - FastAPI TestClient fixture using httpx
       - Playwright browser_context_args fixture with iPad emulation (viewport 1024x1366, device_scale_factor 2, is_mobile True, has_touch True, iPad Safari user agent)
       - A live_server fixture that starts uvicorn in a background thread on a free port for E2E tests
    9. Create tests/test_app.py with the behavior tests above
    10. Run `uv run pytest tests/test_app.py -x` -- all tests must pass
  </action>
  <verify>
    <automated>cd /Users/chriskaschner/Documents/GitHub/mermaids && uv run pytest tests/test_app.py -x -v</automated>
  </verify>
  <done>pyproject.toml exists with all deps, `uv run pytest tests/test_app.py` passes, FastAPI serves frontend/ at localhost:8000</done>
</task>

<task type="auto">
  <name>Task 2: iPad-optimized HTML shell and CSS foundation</name>
  <files>frontend/index.html, frontend/css/style.css</files>
  <action>
    1. Create frontend/index.html with the full iPad web app shell:
       - DOCTYPE html, lang="en", charset UTF-8
       - Viewport meta: width=device-width, initial-scale=1, maximum-scale=1, user-scalable=no, viewport-fit=cover
       - apple-mobile-web-app-capable: yes
       - apple-mobile-web-app-status-bar-style: black-translucent
       - Title: "Mermaid Create &amp; Play"
       - Link to css/style.css
       - Nav bar (#nav-bar) with three nav links: home (#/home), dressup (#/dressup), coloring (#/coloring). Each link has class="nav-icon", data-view attribute, aria-label. Use simple inline SVG icons (house, sparkle/wand, paintbrush) -- keep them minimal, they will be refined later. Each icon link must be at least 60x60px per FOUN-01.
       - Main container (#app) for view content
       - Script tag type="module" src="js/app.js"
    2. Create frontend/css/style.css with full iPad foundation:
       - Reset: margin 0, padding 0, box-sizing border-box, -webkit-tap-highlight-color transparent
       - html/body: width/height 100%, overflow hidden, position fixed, overscroll-behavior none, touch-action manipulation, background #e8f4f8 (soft ocean blue)
       - #app: width 100%, height calc(100% - 80px), overflow hidden
       - #nav-bar: flex, center, gap 40px, padding 10px, height 80px, background rgba(255,255,255,0.8), position at bottom of screen (for child thumb reach)
       - .nav-icon: 60x60px min-width/min-height (FOUN-01), flex center, border-radius 16px, touch-action manipulation
       - .nav-icon.active: background rgba(100,200,255,0.3)
       - [data-region]: cursor pointer, touch-action manipulation
       - [data-region]:active: opacity 0.8
       - Sparkle animation keyframes (sparkle-fade): 0% opacity 1 scale(0), 50% opacity 1 scale(1.2), 100% opacity 0 scale(0.5) translateY(-10px). Duration 0.6s ease-out forwards. pointer-events none on .sparkle class.
    3. Create an empty frontend/js/app.js placeholder (will be built in plan 03)
    4. Verify: `uv run pytest tests/test_app.py -x` still passes (HTML is valid, CSS loads)
  </action>
  <verify>
    <automated>cd /Users/chriskaschner/Documents/GitHub/mermaids && uv run pytest tests/test_app.py -x -v</automated>
  </verify>
  <done>index.html has iPad viewport meta + web-app-capable + nav bar + app container. style.css has full-screen reset, overscroll prevention, 60pt tap target styles, sparkle animation keyframes. All existing tests still pass.</done>
</task>

</tasks>

<verification>
- `uv run pytest tests/test_app.py -x -v` passes
- `uv run python -c "from mermaids.app import app; print(app.title)"` prints "FastAPI"
- frontend/index.html contains apple-mobile-web-app-capable meta tag
- frontend/css/style.css contains touch-action and overscroll-behavior rules
</verification>

<success_criteria>
- FastAPI app serves frontend/ directory at root
- HTML shell is iPad-optimized (viewport, web-app-capable, overscroll prevention)
- CSS has 60pt minimum tap target styles, sparkle animation keyframes
- Test infrastructure works: pytest + conftest with FastAPI and Playwright fixtures
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
