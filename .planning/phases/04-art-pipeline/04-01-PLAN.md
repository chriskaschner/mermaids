---
phase: 04-art-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - src/mermaids/pipeline/config.py
  - src/mermaids/pipeline/prompts.py
  - src/mermaids/pipeline/generate.py
  - scripts/generate_coloring.py
  - scripts/trace_all.py
  - tests/test_generate.py
  - tests/test_trace_coloring.py
autonomous: true
requirements: [ARTP-01, ARTP-02]
user_setup:
  - service: openai
    why: "Image generation via gpt-image-1 API"
    env_vars:
      - name: OPENAI_API_KEY
        source: "OpenAI Dashboard -> API keys (https://platform.openai.com/api-keys)"

must_haves:
  truths:
    - "Running generate_coloring.py produces 4 kawaii mermaid coloring page PNGs in assets/generated/png/coloring/"
    - "Running trace_all.py converts those PNGs to clean SVG line art in assets/generated/svg/coloring/"
    - "Re-running generate_coloring.py skips already-generated files (idempotent)"
    - "API failures are retried 3 times with exponential backoff"
  artifacts:
    - path: "src/mermaids/pipeline/config.py"
      provides: "Shared constants (paths, retry config, image sizes)"
      exports: ["GENERATED_PNG_DIR", "GENERATED_SVG_DIR", "FRONTEND_SVG_DIR", "RETRY_MAX", "RETRY_BASE_DELAY"]
    - path: "src/mermaids/pipeline/prompts.py"
      provides: "Prompt templates for kawaii mermaid art generation"
      exports: ["COLORING_BASE_PROMPT", "COLORING_PAGES", "DRESSUP_BASE_PROMPT", "DRESSUP_VARIANTS"]
    - path: "src/mermaids/pipeline/generate.py"
      provides: "OpenAI API generation with retry and idempotent skip"
      exports: ["generate_image", "generate_coloring_pages"]
    - path: "scripts/generate_coloring.py"
      provides: "CLI entry point for coloring page generation"
    - path: "scripts/trace_all.py"
      provides: "CLI entry point for tracing all generated PNGs to SVGs"
    - path: "tests/test_generate.py"
      provides: "Tests for OpenAI generation logic (mocked API)"
    - path: "tests/test_trace_coloring.py"
      provides: "Tests for tracing coloring page PNGs to SVGs"
  key_links:
    - from: "src/mermaids/pipeline/generate.py"
      to: "openai.OpenAI client"
      via: "client.images.generate() with b64_json response"
      pattern: "client\\.images\\.generate"
    - from: "scripts/generate_coloring.py"
      to: "src/mermaids/pipeline/generate.py"
      via: "import generate_coloring_pages"
      pattern: "from mermaids\\.pipeline\\.generate import"
    - from: "scripts/trace_all.py"
      to: "src/mermaids/pipeline/trace.py"
      via: "import trace_to_svg"
      pattern: "from mermaids\\.pipeline\\.trace import"
---

<objective>
Build the shared pipeline infrastructure and coloring page generation workflow: add OpenAI SDK dependency, create config/prompt modules, implement image generation with retry and idempotency, and trace generated PNGs to SVGs.

Purpose: Establishes the foundation for all art generation (shared by both coloring and dress-up workflows) and delivers the first complete pipeline stage -- generating and tracing kawaii mermaid coloring pages.

Output: 4 coloring page PNGs generated via gpt-image-1, traced to SVG line art via vtracer, with full test coverage of generation logic (mocked API) and tracing.
</objective>

<execution_context>
@/Users/chriskaschner/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chriskaschner/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-art-pipeline/04-CONTEXT.md
@.planning/phases/04-art-pipeline/04-RESEARCH.md

@src/mermaids/pipeline/trace.py
@pyproject.toml

<interfaces>
<!-- Existing trace.py function signature -->
From src/mermaids/pipeline/trace.py:
```python
def trace_to_svg(
    input_path: str | Path,
    output_path: str | Path,
    *,
    simplify: bool = True,
) -> Path:
```
- simplify=True -> binary colormode, filter_speckle=20 (for coloring page outlines)
- simplify=False -> color colormode, filter_speckle=10 (for dress-up parts)

<!-- Existing coloring page file paths expected by frontend -->
From frontend/js/coloring.js:
```javascript
{ id: "page-1-ocean", file: "assets/svg/coloring/page-1-ocean.svg" },
{ id: "page-2-castle", file: "assets/svg/coloring/page-2-castle.svg" },
{ id: "page-3-seahorse", file: "assets/svg/coloring/page-3-seahorse.svg" },
{ id: "page-4-coral", file: "assets/svg/coloring/page-4-coral.svg" },
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pipeline shared modules and test scaffolds</name>
  <files>
    pyproject.toml,
    src/mermaids/pipeline/config.py,
    src/mermaids/pipeline/prompts.py,
    src/mermaids/pipeline/generate.py,
    tests/test_generate.py
  </files>
  <behavior>
    - test_generate_image_calls_api: generate_image() calls client.images.generate with correct model, prompt, size params and returns decoded bytes
    - test_generate_image_skips_existing: generate_image() returns early without API call when output file already exists
    - test_generate_image_retries_on_rate_limit: generate_image() retries up to 3 times on RateLimitError with increasing delays
    - test_generate_image_retries_on_api_error: generate_image() retries up to 3 times on APIError
    - test_generate_image_raises_after_max_retries: generate_image() raises after exhausting retry attempts
    - test_generate_coloring_pages_produces_all_four: generate_coloring_pages() calls generate_image for each of the 4 coloring page definitions
    - test_coloring_prompts_are_bw_outline: COLORING_BASE_PROMPT contains "black and white" and "outline" keywords
    - test_config_paths_are_pathlib: All path constants in config.py are Path objects
  </behavior>
  <action>
    1. Add `openai>=2.0` to pyproject.toml dependencies (not dev deps -- pipeline is production tooling). Run `uv sync` after.

    2. Create `src/mermaids/pipeline/config.py` with:
       - `PROJECT_ROOT`: Path to repo root (resolved from this file's location, 3 parents up)
       - `GENERATED_PNG_DIR`: `PROJECT_ROOT / "assets" / "generated" / "png"` (with subdirs coloring/, dressup/)
       - `GENERATED_SVG_DIR`: `PROJECT_ROOT / "assets" / "generated" / "svg"` (with subdirs coloring/, dressup/)
       - `FRONTEND_SVG_DIR`: `PROJECT_ROOT / "frontend" / "assets" / "svg"`
       - `RETRY_MAX = 3`
       - `RETRY_BASE_DELAY = 2.0`
       - `IMAGE_SIZE = "1024x1024"`

    3. Create `src/mermaids/pipeline/prompts.py` with:
       - `COLORING_BASE_PROMPT`: "Black and white coloring page outline art. Clean simple lines on white background. Chibi kawaii mermaid character with big head, small body, oversized round eyes, minimal detail, Sanrio-like cute style. No shading, no gradients, no fills -- only clean black outlines suitable for a child to color in. "
       - `COLORING_PAGES`: list of dicts with id and prompt_detail keys:
         - page-1-ocean: "swimming in the ocean with small fish and bubbles around her"
         - page-2-castle: "sitting on a rock near an underwater castle with towers and arches"
         - page-3-seahorse: "riding a cute seahorse friend with seaweed in the background"
         - page-4-coral: "playing among coral reef formations with starfish on the ground"
       - `DRESSUP_BASE_PROMPT`: prompt for full-color chibi kawaii mermaid with flat fills (used by Plan 02)
       - `DRESSUP_VARIANTS`: dict of variant definitions for tails, hair, accessories (used by Plan 02)

    4. Create `src/mermaids/pipeline/generate.py` with:
       - `_get_client()`: returns `openai.OpenAI()` (reads OPENAI_API_KEY from env). Cached module-level.
       - `retry_api_call(fn, max_retries=RETRY_MAX, base_delay=RETRY_BASE_DELAY)`: wraps fn() with exponential backoff retry on `openai.RateLimitError` and `openai.APIError`. Prints retry info. Raises on exhaustion.
       - `generate_image(prompt, output_path, *, size=IMAGE_SIZE, quality="high")`: Check if output_path exists (skip if so, print skip message). Otherwise call `client.images.generate(model="gpt-image-1", prompt=prompt, size=size, quality=quality)` via retry_api_call. Decode b64_json response. Write bytes to output_path (ensure parent dirs exist). Return output_path.
       - `generate_coloring_pages()`: For each page in COLORING_PAGES, call generate_image with COLORING_BASE_PROMPT + page["prompt_detail"], output to GENERATED_PNG_DIR / "coloring" / f"{page['id']}.png". Return list of output paths.

    5. Create `tests/test_generate.py` with mocked OpenAI client (mock `openai.OpenAI` or patch `generate._get_client`). Use `unittest.mock.patch` and `tmp_path` fixtures. Create a mock response object with `data[0].b64_json` returning a base64-encoded 1x1 PNG. Test all behaviors listed above.
  </action>
  <verify>
    <automated>cd /Users/chriskaschner/Documents/GitHub/mermaids && uv run pytest tests/test_generate.py -x -v</automated>
  </verify>
  <done>
    - openai SDK added to pyproject.toml and installed
    - config.py exports all path constants and retry settings
    - prompts.py exports coloring and dress-up prompt templates
    - generate.py implements idempotent generation with retry
    - All test_generate.py tests pass with mocked API
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Coloring page tracing script and integration test</name>
  <files>
    scripts/generate_coloring.py,
    scripts/trace_all.py,
    tests/test_trace_coloring.py
  </files>
  <behavior>
    - test_trace_coloring_page_produces_svg: tracing a coloring page PNG (binary mode) produces valid SVG with path elements
    - test_trace_coloring_page_uses_simplify_true: coloring pages are traced with simplify=True (binary mode for clean outlines)
    - test_trace_all_coloring_skips_existing: trace_all skips files where output SVG already exists
    - test_traced_svg_under_500kb: traced coloring page SVGs are under 500KB
  </behavior>
  <action>
    1. Create `scripts/generate_coloring.py`:
       - Thin CLI script: imports generate_coloring_pages from mermaids.pipeline.generate
       - `if __name__ == "__main__":` calls generate_coloring_pages(), prints summary of generated/skipped files
       - No argparse needed -- this is a simple entry point

    2. Create `scripts/trace_all.py`:
       - Imports trace_to_svg from mermaids.pipeline.trace and config paths from mermaids.pipeline.config
       - `trace_coloring_pages()`: scans GENERATED_PNG_DIR / "coloring" for .png files, traces each to GENERATED_SVG_DIR / "coloring" / same_name.svg with simplify=True. Skips if output SVG already exists. Returns list of output paths.
       - `trace_dressup_parts()`: (stub for Plan 02) scans GENERATED_PNG_DIR / "dressup" / "parts" for .png files, traces each to GENERATED_SVG_DIR / "dressup" / same_name.svg with simplify=False. Returns list of output paths.
       - `if __name__ == "__main__":` calls trace_coloring_pages() and trace_dressup_parts(), prints summary.

    3. Create `tests/test_trace_coloring.py`:
       - Create a fixture that generates a simple B&W test PNG (200x200 white background with a black circle drawn via Pillow ImageDraw) to simulate a coloring page.
       - Test that tracing with simplify=True produces valid SVG (parse XML, check svg root).
       - Test that output is under 500KB.
       - Test idempotent skip logic (check that trace_to_svg is not called when output already exists -- use a wrapper function or check file modification time).

    4. Make scripts/ directory importable: ensure both scripts can be run with `uv run python scripts/generate_coloring.py` and `uv run python scripts/trace_all.py`.
  </action>
  <verify>
    <automated>cd /Users/chriskaschner/Documents/GitHub/mermaids && uv run pytest tests/test_trace_coloring.py tests/test_generate.py -x -v</automated>
  </verify>
  <done>
    - scripts/generate_coloring.py is runnable and calls generate_coloring_pages()
    - scripts/trace_all.py traces coloring PNGs to SVGs with simplify=True and has a stub for dress-up tracing
    - All tracing tests pass (valid SVG output, size under 500KB, idempotent skip)
    - Full test suite: uv run pytest tests/test_generate.py tests/test_trace_coloring.py tests/test_pipeline.py -x all green
  </done>
</task>

</tasks>

<verification>
- `uv run pytest tests/test_generate.py tests/test_trace_coloring.py tests/test_pipeline.py -x -v` -- all tests pass
- `uv run python -c "from mermaids.pipeline.config import GENERATED_PNG_DIR; print(GENERATED_PNG_DIR)"` -- prints valid path
- `uv run python -c "from mermaids.pipeline.generate import generate_coloring_pages; print('import ok')"` -- no import errors
- `uv run python -c "from mermaids.pipeline.prompts import COLORING_BASE_PROMPT; assert 'outline' in COLORING_BASE_PROMPT.lower()"` -- prompt validates
</verification>

<success_criteria>
- OpenAI SDK installed and importable
- Config, prompts, and generate modules exist with correct exports
- generate_image() has retry (3x exponential backoff) and idempotent skip
- generate_coloring_pages() produces 4 PNG paths in assets/generated/png/coloring/
- trace_all.py traces coloring PNGs to SVGs with simplify=True (binary mode)
- All tests pass with mocked API (no real API calls in tests)
</success_criteria>

<output>
After completion, create `.planning/phases/04-art-pipeline/04-01-SUMMARY.md`
</output>
