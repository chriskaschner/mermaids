---
phase: 01-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/mermaids/pipeline/__init__.py
  - src/mermaids/pipeline/trace.py
  - assets/source/mermaid-proof-of-concept.png
  - frontend/assets/svg/mermaid.svg
  - tests/test_pipeline.py
autonomous: true
requirements:
  - FOUN-03

must_haves:
  truths:
    - "vtracer converts a raster mermaid image to a clean SVG file under 200KB"
    - "The output SVG has named <g> groups with data-region attributes for tail, hair, and body"
    - "Each tappable region has an invisible hit-area rect ensuring 60pt+ tap targets"
    - "The SVG has a watercolor filter defined in <defs> for visual style"
  artifacts:
    - path: "src/mermaids/pipeline/trace.py"
      provides: "vtracer wrapper for raster-to-SVG conversion"
      exports: ["trace_to_svg"]
    - path: "frontend/assets/svg/mermaid.svg"
      provides: "Proof-of-concept mermaid SVG with tappable regions"
      contains: "data-region"
    - path: "tests/test_pipeline.py"
      provides: "Pipeline tests for SVG tracing"
  key_links:
    - from: "src/mermaids/pipeline/trace.py"
      to: "vtracer"
      via: "vtracer.convert_image_to_svg_py"
      pattern: "vtracer\\.convert"
    - from: "frontend/assets/svg/mermaid.svg"
      to: "frontend/index.html"
      via: "Will be inlined into the dress-up view (plan 03)"
      pattern: "data-region"
---

<objective>
Build the art asset pipeline (raster -> SVG via vtracer) and create a proof-of-concept mermaid SVG with tappable regions, watercolor filter, and expanded hit areas.

Purpose: The art pipeline is the highest-risk unknown in Phase 1. Validating it early unblocks everything else. The proof-of-concept SVG (not final art) proves the pipeline works and gives plans 03 a real SVG to wire touch events to.
Output: A trace_to_svg() function, a working mermaid.svg with named regions, and passing pipeline tests.
</objective>

<execution_context>
@/Users/chriskaschner/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chriskaschner/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: vtracer SVG tracing pipeline</name>
  <files>src/mermaids/pipeline/__init__.py, src/mermaids/pipeline/trace.py, tests/test_pipeline.py</files>
  <behavior>
    - Test: trace_to_svg() with a simple test image (create a 200x400 colored rectangle PNG using Pillow) produces a valid SVG file
    - Test: Output SVG file size is under 500KB for a 1024px source image
    - Test: Output SVG contains valid XML with an svg root element
    - Test: trace_to_svg() with simplify=True produces fewer paths than simplify=False
    - Test: Source images larger than 1024px are resized before tracing
  </behavior>
  <action>
    1. Create src/mermaids/pipeline/__init__.py (empty)
    2. Create src/mermaids/pipeline/trace.py with trace_to_svg() function:
       - Accepts input_path (str|Path), output_path (str|Path), simplify (bool, default True)
       - Opens image with Pillow, resizes to max 1024px dimension if larger (LANCZOS resampling)
       - Saves preprocessed image to a temp file
       - Calls vtracer.convert_image_to_svg_py with these settings:
         - colormode='color' (or 'binary' if simplify=True -- for Phase 1 silhouette)
         - hierarchical='stacked'
         - mode='spline'
         - filter_speckle=10 (20 if simplify)
         - color_precision=4
         - layer_difference=32
         - corner_threshold=60
         - length_threshold=4.0
         - max_iterations=10
         - splice_threshold=45
         - path_precision=3
       - Cleans up temp file
       - Returns output_path as Path
    3. Create tests/test_pipeline.py:
       - Fixture that creates a simple test PNG (200x400 colored rectangle) using Pillow
       - Tests from the behavior block above
       - Use tmp_path fixture for output files
    4. Run tests: `uv run pytest tests/test_pipeline.py -x -v`
  </action>
  <verify>
    <automated>cd /Users/chriskaschner/Documents/GitHub/mermaids && uv run pytest tests/test_pipeline.py -x -v</automated>
  </verify>
  <done>trace_to_svg() converts raster images to SVG, handles resize, respects simplify flag. All pipeline tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Proof-of-concept mermaid SVG with tappable regions</name>
  <files>frontend/assets/svg/mermaid.svg</files>
  <action>
    Since this is a proof-of-concept (per user decision: "simple mermaid silhouette divided into tappable regions -- proves the SVG + touch pipeline works without needing final art"), create the SVG by hand rather than running the full AI-image pipeline. The pipeline was validated in Task 1; the proof-of-concept SVG needs specific structure (named groups, hit areas) that manual creation guarantees.

    Create frontend/assets/svg/mermaid.svg:
    1. Set viewBox="0 0 400 700" with preserveAspectRatio="xMidYMid meet"
    2. Add a <defs> section with the watercolor SVG filter (id="watercolor"):
       - feTurbulence type="fractalNoise" baseFrequency="0.04" numOctaves=4 seed=2 result="noise"
       - feDisplacementMap in="SourceGraphic" in2="noise" scale=8 xChannelSelector="R" yChannelSelector="G" result="displaced"
       - feTurbulence type="fractalNoise" baseFrequency="0.8" numOctaves=3 result="fine-noise"
       - feComposite in="displaced" in2="fine-noise" operator="in"
    3. Wrap all mermaid art in <g filter="url(#watercolor)">
    4. Create three tappable region groups inside:

       a. <g data-region="hair" pointer-events="all">
          - Invisible hit-area <rect> covering at least 80x80 area around the hair zone (top portion, roughly y=20 to y=150)
          - Visible paths: simple flowing hair shape, fill with soft lavender (#c4a7d7)

       b. <g data-region="body" pointer-events="all">
          - Invisible hit-area <rect> covering at least 120x200 area around the torso (roughly y=120 to y=380)
          - Visible paths: simple torso/arms silhouette, fill with warm peach (#f5c8a8)

       c. <g data-region="tail" pointer-events="all">
          - Invisible hit-area <rect> covering at least 120x250 area around the tail (roughly y=350 to y=680)
          - Visible paths: simple mermaid tail shape with fin, fill with ocean teal (#7ec8c8)

    5. All invisible rects: fill="none" stroke="none" (truly invisible but catches pointer events)
    6. Use soft, rounded path shapes -- this is a children's app. The silhouette should look friendly, not angular.
    7. Keep total file under 50KB (it's a simple silhouette, should be well under)

    The SVG should be a recognizable mermaid silhouette that a 6-year-old would identify as a mermaid: flowing hair, human upper body, fish tail with a fin at the bottom.
  </action>
  <verify>
    <automated>cd /Users/chriskaschner/Documents/GitHub/mermaids && python3 -c "
import xml.etree.ElementTree as ET
tree = ET.parse('frontend/assets/svg/mermaid.svg')
root = tree.getroot()
ns = {'svg': 'http://www.w3.org/2000/svg'}
regions = root.findall('.//*[@data-region]')
names = [r.get('data-region') for r in regions]
assert 'hair' in names, 'Missing hair region'
assert 'body' in names, 'Missing body region'
assert 'tail' in names, 'Missing tail region'
assert root.get('viewBox') is not None, 'Missing viewBox'
import os
size = os.path.getsize('frontend/assets/svg/mermaid.svg')
assert size < 50000, f'SVG too large: {size} bytes'
print(f'OK: {len(regions)} regions, {size} bytes, viewBox={root.get(\"viewBox\")}')
"</automated>
  </verify>
  <done>mermaid.svg exists with 3 named tappable regions (hair, body, tail), each with invisible hit-area rects, watercolor filter in defs, viewBox set, file under 50KB. SVG is a recognizable mermaid silhouette.</done>
</task>

</tasks>

<verification>
- `uv run pytest tests/test_pipeline.py -x -v` passes
- frontend/assets/svg/mermaid.svg has 3 data-region groups (hair, body, tail)
- SVG has watercolor filter defined in defs
- SVG file size under 50KB
- SVG is valid XML with viewBox attribute
</verification>

<success_criteria>
- trace_to_svg() function converts raster images to SVG with configurable simplification
- Proof-of-concept mermaid SVG has tappable regions with expanded hit areas
- Watercolor filter applied for visual style
- Pipeline tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
