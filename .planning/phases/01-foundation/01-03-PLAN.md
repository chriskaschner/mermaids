---
phase: 01-foundation
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
  - 01-02
files_modified:
  - frontend/js/app.js
  - frontend/js/touch.js
  - frontend/js/sparkle.js
  - frontend/index.html
  - tests/test_e2e.py
autonomous: false
requirements:
  - FOUN-04
  - NAVG-01
  - NAVG-02

must_haves:
  truths:
    - "Home screen shows two large icon buttons (dress-up and coloring) and tapping either navigates to that activity screen"
    - "Navigation icons are visible from every activity screen and switching between them works with a single tap"
    - "Tapping an SVG region on the mermaid triggers a sparkle particle effect that fades after ~0.5s"
    - "All interactive elements have tap targets of 60pt or larger"
    - "Touch interaction works with iPad emulation (pointer events, no 300ms delay)"
  artifacts:
    - path: "frontend/js/app.js"
      provides: "Hash-based SPA router with view rendering"
      contains: "hashchange"
    - path: "frontend/js/touch.js"
      provides: "SVG touch event handling with expanded hit areas"
      contains: "pointerdown"
    - path: "frontend/js/sparkle.js"
      provides: "Sparkle particle effect on SVG tap"
      contains: "createElementNS"
    - path: "tests/test_e2e.py"
      provides: "Playwright E2E tests with iPad emulation"
  key_links:
    - from: "frontend/js/app.js"
      to: "window.location.hash"
      via: "hashchange event listener"
      pattern: "addEventListener.*hashchange"
    - from: "frontend/js/touch.js"
      to: "frontend/assets/svg/mermaid.svg"
      via: "pointerdown on #mermaid-svg delegating to [data-region]"
      pattern: "closest.*data-region"
    - from: "frontend/js/touch.js"
      to: "frontend/js/sparkle.js"
      via: "import and call triggerSparkle on region tap"
      pattern: "triggerSparkle"
    - from: "frontend/js/app.js"
      to: "frontend/js/touch.js"
      via: "import and call initTouch after rendering dress-up view"
      pattern: "initTouch"
---

<objective>
Wire up the interactive frontend: hash-based routing between home/dressup/coloring views, touch event handling on SVG regions, sparkle particle feedback, and a home screen with large activity buttons.

Purpose: This is where the app comes alive -- a child can tap the screen and see sparkles, navigate between activities, and interact with the mermaid. Covers the remaining Phase 1 requirements (FOUN-04, NAVG-01, NAVG-02).
Output: A fully interactive SPA with routing, touch feedback, and E2E tests proving it works under iPad emulation.
</objective>

<execution_context>
@/Users/chriskaschner/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chriskaschner/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
@.planning/phases/01-foundation/01-02-SUMMARY.md

<interfaces>
<!-- From plan 01-01: HTML shell structure -->
frontend/index.html provides:
  - nav element with id="nav-bar" containing .nav-icon links with data-view attributes
  - main element with id="app" as container for view content
  - Script loads frontend/js/app.js as type="module"

frontend/css/style.css provides:
  - .nav-icon: 60x60px min, flex center, touch-action manipulation
  - .nav-icon.active: highlighted state
  - [data-region]: pointer cursor, touch-action manipulation
  - .sparkle: animation sparkle-fade 0.6s ease-out forwards, pointer-events none
  - @keyframes sparkle-fade defined

<!-- From plan 01-02: SVG structure -->
frontend/assets/svg/mermaid.svg provides:
  - viewBox="0 0 400 700" with preserveAspectRatio="xMidYMid meet"
  - defs with watercolor filter id="watercolor"
  - g[data-region="hair"] with pointer-events="all", hit-area rect + visible paths
  - g[data-region="body"] with pointer-events="all", hit-area rect + visible paths
  - g[data-region="tail"] with pointer-events="all", hit-area rect + visible paths
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Hash router, views, touch interaction, and sparkle</name>
  <files>frontend/js/app.js, frontend/js/touch.js, frontend/js/sparkle.js, frontend/index.html, tests/test_e2e.py</files>
  <behavior>
    - Test: App loads at / and redirects to /#/home (or displays home view)
    - Test: Home view shows two large activity buttons (dress-up and coloring), each at least 60x60px
    - Test: Tapping dress-up button navigates to /#/dressup
    - Test: Tapping coloring button navigates to /#/coloring
    - Test: Dress-up view displays the inline mermaid SVG
    - Test: Navigation icons are visible on every view (home, dressup, coloring)
    - Test: Tapping a nav icon switches to the corresponding view
    - Test: Tapping an SVG region (e.g., tail) creates sparkle elements that appear and then are removed
    - Test: All data-region elements have bounding boxes of at least 60x60
    - Test: All nav-icon elements have bounding boxes of at least 60x60
  </behavior>
  <action>
    1. Create tests/test_e2e.py FIRST with failing tests for all behavior above:
       - Import pytest, playwright sync_api Page, expect
       - Tests use the live_server fixture from conftest.py (starts FastAPI on a free port)
       - test_home_view_loads: page.goto, expect #app to contain activity buttons
       - test_home_activity_buttons_size: check bounding box >= 60x60 for each
       - test_navigate_to_dressup: tap dress-up button, expect URL contains dressup
       - test_navigate_to_coloring: tap coloring button, expect URL contains coloring
       - test_dressup_shows_mermaid_svg: navigate to dressup, expect SVG with data-region visible
       - test_nav_visible_on_all_views: for each route, check nav-bar is visible
       - test_nav_switching: from dressup, tap coloring nav icon, expect coloring view
       - test_tap_region_triggers_sparkle: navigate to dressup, tap [data-region="tail"], expect .sparkle elements to appear
       - test_sparkle_cleanup: after tap, wait 700ms, expect sparkle elements removed from DOM
       - test_tap_targets_minimum_size: check all [data-region] and .nav-icon elements >= 60x60
    2. Run tests -- they should all FAIL (no JS yet)
    3. Create frontend/js/sparkle.js:
       - Export function triggerSparkle(svgElement, event)
       - Get SVG coordinate from event using createSVGPoint + getScreenCTM().inverse()
       - Create 6 small circle elements (SVG namespace) at randomized positions around tap point (+/- 20px)
       - Each circle: r=2-5 random, fill="gold", class="sparkle"
       - Append to the SVG root element
       - setTimeout to remove each sparkle after 600ms
    4. Create frontend/js/touch.js:
       - Export function initTouch(svgSelector)
       - Attach a single pointerdown listener to the SVG container (event delegation)
       - On pointerdown: use event.target.closest('[data-region]') to find the tapped region
       - If no region found, return (ignore background taps -- per Claude's discretion, keeping it simple for Phase 1)
       - If region found: call triggerSparkle(region, event) imported from sparkle.js
       - Also add a brief opacity pulse on the tapped region (set opacity 0.7, restore to 1.0 after 200ms) for immediate visual feedback per FOUN-04
    5. Create frontend/js/app.js:
       - Import initTouch from './touch.js'
       - Define route map: '' -> renderHome, 'home' -> renderHome, 'dressup' -> renderDressUp, 'coloring' -> renderColoring
       - router() function: read hash (window.location.hash.slice(2) || 'home'), call matching render function, call updateNavHighlight(hash)
       - updateNavHighlight(hash): remove .active from all .nav-icon, add .active to the one with matching data-view
       - renderHome(): set #app innerHTML to two large activity buttons:
         - Dress-up button: anchor linking to #/dressup wrapping a large (120x120px+) inline SVG icon (sparkle/wand shape) with a text-free label. Add class="activity-btn" for styling.
         - Coloring button: anchor linking to #/coloring wrapping a large (120x120px+) inline SVG icon (paintbrush shape). Add class="activity-btn".
         - Style activity buttons: centered grid or flex layout, each button 120x120px minimum, border-radius 24px, soft pastel backgrounds, touch-action manipulation
       - renderDressUp(): set #app innerHTML to the mermaid SVG inlined (fetch the SVG file content and insert it, or include it directly as a template string -- template string is simpler for Phase 1 since there is no build step). The SVG must have id="mermaid-svg" on the root element. After inserting, call initTouch('#mermaid-svg').
       - renderColoring(): set #app innerHTML to a placeholder message (styled appropriately -- "Coming soon!" centered in a large, friendly font). This view will be built in Phase 3.
       - Attach hashchange and DOMContentLoaded listeners to call router()
    6. Update frontend/index.html: ensure the nav bar icons have proper data-view attributes matching the routes. The nav bar should be at the BOTTOM of the screen (within thumb reach for a child holding an iPad). On the home screen, the nav bar can be hidden or simplified since the home screen IS the navigation.
    7. Add CSS for .activity-btn to frontend/css/style.css (or add a style block if plan 01 CSS file is not yet committed -- but prefer modifying the CSS file):
       - min-width: 120px, min-height: 120px
       - border-radius: 24px
       - display: flex, align-items center, justify-content center
       - background: soft pastel color
       - touch-action: manipulation
       - No text decoration
    8. Run tests: `uv run pytest tests/test_e2e.py -x -v` -- all should pass
    9. Run full suite: `uv run pytest tests/ -x -v` -- everything green
  </action>
  <verify>
    <automated>cd /Users/chriskaschner/Documents/GitHub/mermaids && uv run pytest tests/test_e2e.py -x -v</automated>
  </verify>
  <done>Hash router navigates between home/dressup/coloring. Home screen has two large activity buttons (60pt+). Dress-up view shows inline mermaid SVG. Tapping SVG regions triggers gold sparkle particles that fade after 0.6s. Nav icons visible and functional on all views. All E2E tests pass under iPad emulation.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of complete Phase 1 foundation</name>
  <files>none</files>
  <action>
    Human verifies the complete Phase 1 foundation: iPad-optimized web app with mermaid SVG, touch sparkle feedback, and navigation between home/dressup/coloring screens.

    Steps to verify:
    1. Start the server: `cd /Users/chriskaschner/Documents/GitHub/mermaids and run uv run uvicorn mermaids.app:app --host 0.0.0.0 --port 8000`
    2. Open http://localhost:8000 in a browser (or on an iPad if available)
    3. Verify home screen shows two large activity buttons (dress-up and coloring)
    4. Tap "Dress Up" -- should navigate to a view showing a mermaid silhouette
    5. Tap on the mermaid's tail, hair, or body -- gold sparkle particles should appear and fade
    6. Check the navigation bar at the bottom -- tap "Coloring" to switch views
    7. From coloring screen, tap "Home" to return
    8. Overall: Does it feel like a children's app? Are buttons large enough? Is the watercolor filter visible on the mermaid?
  </action>
  <verify>Human visual confirmation</verify>
  <done>User approves the visual appearance, interaction feel, and navigation flow. App feels like a children's creative activity, not a website.</done>
</task>

</tasks>

<verification>
- `uv run pytest tests/ -v` -- full test suite passes
- Home screen renders two large activity buttons
- Hash routing works: /#/home, /#/dressup, /#/coloring
- Tapping SVG regions produces sparkle feedback
- Navigation works from every screen
- All tap targets >= 60pt
</verification>

<success_criteria>
- All 6 Phase 1 requirements demonstrated:
  - FOUN-01: All tap targets 60pt+ (verified by test_tap_targets_minimum_size)
  - FOUN-02: Works in iPad Safari emulation (all E2E tests run with iPad config)
  - FOUN-03: Watercolor filter applied to mermaid SVG (visual check)
  - FOUN-04: Sparkle + opacity feedback on every tap (verified by test_tap_region_triggers_sparkle)
  - NAVG-01: Home screen has two large activity buttons (verified by test_home_activity_buttons_size)
  - NAVG-02: Nav icons on every screen, single-tap switching (verified by test_nav_switching)
- Full test suite green
- Human approves visual appearance and interaction feel
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-03-SUMMARY.md`
</output>
