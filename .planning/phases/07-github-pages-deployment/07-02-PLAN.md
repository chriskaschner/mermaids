---
phase: 07-github-pages-deployment
plan: 02
type: execute
wave: 2
depends_on:
  - 07-01
files_modified:
  - frontend/CNAME
autonomous: false
requirements:
  - DPLY-01
  - DPLY-03

user_setup:
  - service: github-pages
    why: "Custom domain for static site hosting"
    env_vars: []
    dashboard_config:
      - task: "Set Pages source to GitHub Actions"
        location: "GitHub repo Settings → Pages → Source → GitHub Actions"
      - task: "Set custom domain to mermaids.chriskaschner.com"
        location: "GitHub repo Settings → Pages → Custom domain → mermaids.chriskaschner.com → Save"
      - task: "Enable Enforce HTTPS (once available, up to 24h after DNS propagates)"
        location: "GitHub repo Settings → Pages → Enforce HTTPS checkbox"
  - service: dns
    why: "Route mermaids.chriskaschner.com to GitHub Pages"
    env_vars: []
    dashboard_config:
      - task: "Add CNAME record: mermaids → chriskaschner.github.io (TTL 300)"
        location: "DNS provider for chriskaschner.com domain (wherever the domain is registered)"
      - task: "Verify propagation: dig mermaids.chriskaschner.com CNAME"
        location: "Terminal (run locally after adding DNS record)"

must_haves:
  truths:
    - "https://mermaids.chriskaschner.com loads the app on a real iPad running Safari"
    - "Both dress-up and coloring activities work with touch on the real iPad"
    - "No asset 404s when browsing the live site (all SVGs, JS, CSS load correctly)"
    - "HTTPS is enforced (http:// redirects to https://)"
  artifacts:
    - path: "frontend/CNAME"
      provides: "Custom domain declaration for branch-based fallback (belt-and-suspenders)"
      contains: "mermaids.chriskaschner.com"
  key_links:
    - from: "DNS CNAME record"
      to: "chriskaschner.github.io"
      via: "mermaids.chriskaschner.com CNAME"
      pattern: "dig mermaids.chriskaschner.com CNAME"
    - from: "GitHub Pages Settings"
      to: "mermaids.chriskaschner.com"
      via: "Custom domain field in repo Settings → Pages"
      pattern: "Settings → Pages → Custom domain"
---

<objective>
Configure the custom domain `mermaids.chriskaschner.com` for the GitHub Pages deployment, and perform final iPad Safari verification that the live site works end-to-end.

Purpose: The CI test job (Plan 01) verifies static correctness. This plan does the one-time DNS + Settings UI configuration to make the site accessible on the custom domain, then verifies on a real iPad.
Output: Live site at https://mermaids.chriskaschner.com, confirmed working on real iPad Safari.
</objective>

<execution_context>
@/Users/chriskaschner/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chriskaschner/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/07-github-pages-deployment/07-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create frontend/CNAME for belt-and-suspenders domain declaration</name>
  <files>frontend/CNAME</files>
  <action>
Create `frontend/CNAME` containing exactly one line:

```
mermaids.chriskaschner.com
```

Note: Per official GitHub docs and confirmed community reports, CNAME files are ignored by `actions/deploy-pages` when using GitHub Actions as the Pages source. The custom domain MUST also be configured in repo Settings → Pages → Custom domain field (see user_setup section and Task 2). This CNAME file is belt-and-suspenders in case GitHub's behavior changes, but it is NOT sufficient alone.

After creating the file, verify it contains exactly the domain with no extra content.
  </action>
  <verify>
    <automated>test "$(cat frontend/CNAME)" = "mermaids.chriskaschner.com" &amp;&amp; echo "CNAME OK"</automated>
  </verify>
  <done>frontend/CNAME exists containing exactly "mermaids.chriskaschner.com".</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Configure DNS and GitHub Pages Settings</name>
  <files>n/a</files>
  <action>
Human must complete two one-time configuration steps (no CLI/API access available for these):

Step A — DNS Record (do this first):
1. Log into the DNS provider for chriskaschner.com
2. Add a CNAME record: Name=mermaids, Value=chriskaschner.github.io, TTL=300
3. Verify propagation: `dig mermaids.chriskaschner.com CNAME`
   Expected: mermaids.chriskaschner.com. 300 IN CNAME chriskaschner.github.io.

Step B — GitHub Pages Settings (after DNS propagates):
1. Go to https://github.com/chriskaschner/mermaids/settings/pages
2. Verify Source is "GitHub Actions"
3. In "Custom domain" field, enter: mermaids.chriskaschner.com → Save
4. Wait for green "DNS check successful" status
5. Once available, enable "Enforce HTTPS"
  </action>
  <verify>
    <automated>dig mermaids.chriskaschner.com CNAME +short</automated>
  </verify>
  <done>DNS CNAME record resolves to chriskaschner.github.io. GitHub Pages Settings shows custom domain mermaids.chriskaschner.com with DNS check passing.</done>
  <what-to-do>
Complete two one-time configuration steps that Claude cannot automate (no API access):

**Step A: DNS Record (do this first)**
1. Log into your DNS provider for chriskaschner.com
2. Add a CNAME record:
   - Name/Host: `mermaids`
   - Value/Target: `chriskaschner.github.io`
   - TTL: 300 (or lowest available)
3. Verify propagation (may take 1-10 minutes):
   ```
   dig mermaids.chriskaschner.com CNAME
   ```
   Expected: `mermaids.chriskaschner.com. 300 IN CNAME chriskaschner.github.io.`

**Step B: GitHub Pages Settings (after DNS propagates)**
1. Go to https://github.com/chriskaschner/mermaids/settings/pages
2. Verify Source is already "GitHub Actions" (should be set from existing deploy.yml)
3. In "Custom domain" field, enter: `mermaids.chriskaschner.com`
4. Click Save — GitHub will verify the DNS record
5. Wait for the green checkmark "DNS check successful"
6. Once available (may take up to 24h), check "Enforce HTTPS"

If the DNS check fails, wait a few more minutes for propagation and try saving again.
  </what-to-do>
  <resume-signal>Type "dns-configured" once both DNS and GitHub Pages custom domain are set and the DNS check shows green in GitHub Settings.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify live site on real iPad Safari</name>
  <files>n/a</files>
  <action>
Human verifies the live production site on a real iPad Safari device. This is the final DPLY-03 acceptance gate — Playwright WebKit on Linux emulates but does not replace real iPad Safari for touch/viewport validation.
  </action>
  <verify>
    <automated>curl -sf https://mermaids.chriskaschner.com/ | head -5</automated>
  </verify>
  <done>App loads at https://mermaids.chriskaschner.com on real iPad Safari. Both dress-up and coloring activities work with touch. No asset 404s.</done>
  <what-built>
CI test job (Plan 01) verified the site works against a static file server. DNS and GitHub Pages custom domain are now configured. The deploy.yml will have run on the last push to main, making the site live.
  </what-built>
  <how-to-verify>
1. On a real iPad running Safari, open: https://mermaids.chriskaschner.com
2. Verify the app loads (home/landing screen appears, no blank page or error)
3. Tap into the Dress-Up activity:
   - Mermaid displays in kawaii AI art style
   - Tapping tail/hair/accessory options swaps the part
   - Tapping a color swatch recolors the selected part
4. Navigate back, tap into the Coloring activity:
   - A coloring page renders with crisp outlines
   - Tapping a color swatch then tapping a white region flood-fills that region
   - Undo button reverts the last fill
5. Navigate away and back to coloring — no crash or degraded performance

If the site is not yet live, check https://github.com/chriskaschner/mermaids/actions to confirm the deploy job completed.
  </how-to-verify>
  <resume-signal>Type "verified" if all activities work correctly on iPad Safari, or describe any issues.</resume-signal>
</task>

</tasks>

<verification>
Phase complete when:
- https://mermaids.chriskaschner.com loads the app without errors
- Both dress-up and coloring activities function correctly with touch on real iPad Safari
- HTTPS enforced (or "Enforce HTTPS" checkbox is available and checked)
- CI test job is green on last push to main (visible in GitHub Actions tab)
</verification>

<success_criteria>
- frontend/CNAME contains mermaids.chriskaschner.com
- DNS CNAME record resolves: dig mermaids.chriskaschner.com CNAME returns chriskaschner.github.io
- GitHub Pages custom domain field set to mermaids.chriskaschner.com
- https://mermaids.chriskaschner.com loads the app on iPad Safari
- Both dress-up and coloring work with touch (DPLY-03 satisfied)
- CI test job passes on main branch (DPLY-01, DPLY-02 satisfied via automated E2E)
</success_criteria>

<output>
After completion, create `.planning/phases/07-github-pages-deployment/07-02-SUMMARY.md`
</output>
