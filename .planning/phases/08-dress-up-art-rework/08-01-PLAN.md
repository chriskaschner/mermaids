---
phase: 08-dress-up-art-rework
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/mermaids/pipeline/prompts.py
  - src/mermaids/pipeline/edit.py
  - src/mermaids/pipeline/assemble.py
  - tests/test_masks.py
  - tests/test_assemble.py
autonomous: true
requirements: [DRSU-01, DRSU-02, DRSU-03, DRSU-04, DEBT-03]

must_haves:
  truths:
    - "Pipeline generates 1 base mermaid + 12 part variants (3 hair, 3 eyes, 3 tail, 3 acc)"
    - "Region masks for hair, eyes, tail, and accessories do not overlap"
    - "Assembled SVG has 5 stacked use elements (tail, body, hair, eyes, accessories) instead of 1"
    - "Each use element references a part group in defs, not a full character"
  artifacts:
    - path: "src/mermaids/pipeline/prompts.py"
      provides: "DRESSUP_VARIANTS with eyes category, refined prompts"
      contains: "eyes"
    - path: "src/mermaids/pipeline/edit.py"
      provides: "Non-overlapping REGIONS including eyes, generate_dressup_variants for 4 categories"
      contains: "eyes"
    - path: "src/mermaids/pipeline/assemble.py"
      provides: "Multi-use SVG assembly with 5 stacked layers"
      contains: "active-hair"
  key_links:
    - from: "src/mermaids/pipeline/edit.py"
      to: "src/mermaids/pipeline/prompts.py"
      via: "DRESSUP_VARIANTS import"
      pattern: "from mermaids.pipeline.prompts import.*DRESSUP_VARIANTS"
    - from: "src/mermaids/pipeline/assemble.py"
      to: "frontend/assets/svg/mermaid.svg"
      via: "assemble_mermaid_svg output"
      pattern: "assemble_mermaid_svg"
---

<objective>
Rework the art generation pipeline from "each variant is a full character" to "one base body + swappable isolated parts." Add eyes as a new category. Redesign region masks so hair, eyes, tail, and accessories do not overlap (fixes DEBT-03). Update SVG assembly to produce multi-layer stacked `<use>` structure.

Purpose: The current pipeline generates 9 full-character variants where each is a complete mermaid. The new approach generates 1 static base body + 12 isolated part variants (3 per category), assembled into an SVG with 5 `<use>` layers that can be swapped independently.

Output: Updated pipeline modules (prompts.py, edit.py, assemble.py) and passing tests.
</objective>

<execution_context>
@/Users/chriskaschner/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chriskaschner/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-dress-up-art-rework/08-CONTEXT.md

@src/mermaids/pipeline/prompts.py
@src/mermaids/pipeline/edit.py
@src/mermaids/pipeline/assemble.py
@src/mermaids/pipeline/config.py
@tests/test_masks.py
@tests/test_assemble.py
</context>

<interfaces>
<!-- Current interfaces that will be modified -->

From src/mermaids/pipeline/prompts.py:
```python
DRESSUP_BASE_PROMPT: str  # Full color kawaii mermaid prompt
DRESSUP_VARIANTS: dict[str, list[dict[str, str]]]  # {tails: [...], hair: [...], accessories: [...]}
```

From src/mermaids/pipeline/edit.py:
```python
REGIONS: dict[str, tuple[int, int, int, int]]  # {tail: (x1,y1,x2,y2), hair: ..., acc: ...}
def create_region_mask(image_size, region_bbox) -> bytes
def edit_region(base_image_path, mask_bytes, prompt, output_path) -> Path
def generate_base_mermaid() -> Path
def generate_dressup_variants() -> list[Path]
```

From src/mermaids/pipeline/assemble.py:
```python
VARIANT_IDS: list[str]  # ["tail-1"..."acc-3"]
def assemble_mermaid_svg(traced_parts_dir, output_path) -> Path  # Single <use> structure
def copy_dressup_parts_to_frontend() -> list[Path]
def copy_mermaid_to_frontend(assembled_path) -> Path
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Update prompts and region masks for 4-category isolated parts</name>
  <files>
    src/mermaids/pipeline/prompts.py,
    src/mermaids/pipeline/edit.py,
    tests/test_masks.py
  </files>
  <behavior>
    - Test: DRESSUP_VARIANTS has 4 keys: "tails", "hair", "eyes", "accessories" with 3 variants each (12 total)
    - Test: REGIONS has 4 entries: "tail", "hair", "eyes", "acc" -- all non-overlapping (no bbox intersection)
    - Test: Eyes region fits within the face area (roughly center-upper, smaller than hair region)
    - Test: Hair and tail regions do not overlap vertically (DEBT-03 fix)
    - Test: generate_dressup_variants() produces 12 variant PNGs (not 9)
    - Test: Each variant prompt includes "isolated part only" language to prevent full-character generation
  </behavior>
  <action>
    **prompts.py changes:**
    - Add "eyes" category to DRESSUP_VARIANTS with 3 variants:
      - eye-1: "big round eyes, classic kawaii style" (per CONTEXT.md)
      - eye-2: "sparkle star-pupil eyes" (per CONTEXT.md)
      - eye-3: "sleepy half-closed dreamy eyes" (per CONTEXT.md)
    - Update DRESSUP_BASE_PROMPT to emphasize the base body should be a neutral, front-facing mermaid suitable for part overlay editing
    - Update variant prompt_detail strings to specify "isolated part only, keep rest of character unchanged" so the edit API modifies only the masked region

    **edit.py changes:**
    - Redesign REGIONS dict with 4 non-overlapping bounding boxes:
      - "hair": upper head area (e.g., 150, 0, 874, 300) -- above eyes
      - "eyes": small face-center region (e.g., 350, 280, 674, 420) -- between hair and body
      - "tail": lower body (e.g., 200, 550, 824, 1024) -- well below hair, no overlap
      - "acc": top-of-head/shoulders (e.g., 250, 0, 774, 200) -- accessories like tiara/garland
    - CRITICAL for DEBT-03: Verify hair region y2 < tail region y1 (no vertical overlap)
    - Add "eyes" to _CATEGORY_TO_REGION mapping
    - Update generate_dressup_variants() -- it now iterates 4 categories producing 12 variants total

    **tests/test_masks.py changes:**
    - Add test_regions_do_not_overlap: verify no pair of REGIONS bboxes intersect
    - Add test_eyes_region_exists: verify "eyes" key in REGIONS
    - Update test_generate_dressup_variants_produces_all_nine -> test_generate_dressup_variants_produces_twelve: mock API, verify 12 results with correct IDs (tail-1..3, hair-1..3, eye-1..3, acc-1..3)
  </action>
  <verify>
    <automated>cd /Users/chriskaschner/Documents/GitHub/mermaids && uv run pytest tests/test_masks.py -x -v</automated>
  </verify>
  <done>
    - DRESSUP_VARIANTS has 4 categories with 3 variants each (12 total)
    - REGIONS has 4 non-overlapping bounding boxes including eyes
    - Hair and tail regions have zero vertical overlap (DEBT-03)
    - generate_dressup_variants() produces 12 variants (mocked)
    - All test_masks.py tests pass
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rework SVG assembly for multi-layer stacked use elements</name>
  <files>
    src/mermaids/pipeline/assemble.py,
    tests/test_assemble.py
  </files>
  <behavior>
    - Test: VARIANT_IDS includes all 12 IDs: tail-1..3, hair-1..3, eye-1..3, acc-1..3
    - Test: Assembled SVG has exactly 5 use elements (active-tail, active-body, active-hair, active-eyes, active-acc) stacked in order
    - Test: Layer stacking order is tail (back) > body (static) > hair (mid) > eyes (front) > accessories (top)
    - Test: active-body use element references a "body" group (static base, always visible)
    - Test: Defs contain a "body" group extracted from the base mermaid traced SVG
    - Test: copy_dressup_parts_to_frontend copies 12 files (not 9)
  </behavior>
  <action>
    **assemble.py changes:**
    - Update VARIANT_IDS to include 12 IDs: tail-1..3, hair-1..3, eye-1..3, acc-1..3
    - Add BODY_ID = "body" constant for the static base body group
    - Rework assemble_mermaid_svg() to produce multi-layer structure:
      1. Accept an additional `base_traced_svg` parameter (Path to the traced base body SVG)
      2. Create a "body" group in defs from the base body traced SVG (static, always displayed)
      3. Create variant groups for all 12 part IDs from their traced SVGs
      4. Instead of a single `<use id="active-character">`, create 5 stacked `<use>` elements:
         - `<use id="active-tail" href="#tail-1" data-category="tail" />`
         - `<use id="active-body" href="#body" />` (static, never swapped by UI)
         - `<use id="active-hair" href="#hair-1" data-category="hair" />`
         - `<use id="active-eyes" href="#eye-1" data-category="eyes" />`
         - `<use id="active-acc" href="#acc-1" data-category="acc" />`
      5. Layer order matters for visual stacking: tail at bottom, accessories on top
      6. All `<use>` elements get `pointer-events="all"` except active-body
    - Background stripping logic (_is_background_path, _make_variant_group) stays the same

    **tests/test_assemble.py changes:**
    - Update VARIANT_IDS fixture to include all 12 IDs
    - Update traced_parts_dir fixture to create 12 SVG files + a body.svg for the base
    - Update assembled_svg fixture to pass base_traced_svg parameter
    - Rewrite test_assemble_has_single_use -> test_assemble_has_five_use_elements: verify 5 use elements with correct IDs and stacking order
    - Add test_body_group_in_defs: verify a "body" group exists in defs
    - Add test_layer_stacking_order: verify use elements appear in correct DOM order (tail, body, hair, eyes, acc)
    - Update test_assemble_has_all_variant_ids to expect 12 + body = 13 groups in defs
    - Update test_copies_all_nine_variants -> test_copies_all_twelve_variants
  </action>
  <verify>
    <automated>cd /Users/chriskaschner/Documents/GitHub/mermaids && uv run pytest tests/test_assemble.py -x -v</automated>
  </verify>
  <done>
    - Assembled SVG has 5 stacked use elements in correct layer order
    - Defs contain body group + 12 variant part groups
    - Background stripping still works on all groups
    - copy_dressup_parts_to_frontend handles 12 files
    - All test_assemble.py tests pass
  </done>
</task>

</tasks>

<verification>
All pipeline module tests pass:
```bash
cd /Users/chriskaschner/Documents/GitHub/mermaids && uv run pytest tests/test_masks.py tests/test_assemble.py -x -v
```
</verification>

<success_criteria>
- Pipeline generates 12 isolated part variants + 1 base body (not 9 full characters)
- Region masks for all 4 categories are non-overlapping (DEBT-03 fixed in code)
- SVG assembly produces multi-layer stacked use structure (5 layers)
- All unit tests pass for masks and assembly
</success_criteria>

<output>
After completion, create `.planning/phases/08-dress-up-art-rework/08-01-SUMMARY.md`
</output>
